<nav class="navbar navbar-light bg-white">
  <a class="navbar-brand" href="http://mooc.fi/english.html" alt="MOOC" target="_blank" rel="noopener">
    <span class="icon-mooc"></span>
  </a>

  <ul class="nav navbar-nav hidden-md-down">
    <% sitemap.resources.select{|resource| resource.data.title && resource.data.published != false }.sort_by {|resource| resource.data.title.split(/(\d+)/).map {|a| a =~ /\d+/ ? a.to_i : a }}.each do |resource| %>
      <li class="nav-item <%= current_resource.destination_path == resource.destination_path ? 'active' : '' %>">
        <%= nav_link_to resource.data.title, resource %>
      </li>
    <% end %>
  </ul>

  <div class="float-xs-right navbar__buttons">
    <div class="dropdown" id="mobile-nav-dropdown">
      <button class="btn btn-secondary dropdown-toggle navbar__mobile-nav-toggle" data-toggle="dropdown">
        <i class="fa fa-bars"></i>
      </button>

      <div class="dropdown-menu">
      </div>
    </div>

    <button class="btn btn-primary" id="tmc-login-toggle">Kirjaudu sisään</button>
  </div>
</nav>
